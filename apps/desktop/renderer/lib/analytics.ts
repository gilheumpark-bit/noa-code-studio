/** Product analytics hooks (no-op in desktop unless wired). */
export function trackAIGeneration(_event?: string, _props?: Record<string, unknown>): void {
  /* optional: Vercel Analytics / internal */
}
